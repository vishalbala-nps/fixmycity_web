import React from 'react';
import { Typography, Box } from '@mui/material';

const MyReports = () => (
  <Box p={2}>
    <Typography variant="h4">My Reports</Typography>
    <Typography>Here are your submitted reports.</Typography>
  </Box>
);

export default MyReports;
